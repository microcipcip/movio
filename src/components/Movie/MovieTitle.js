import React from 'react'
import PropTypes from 'prop-types' // eslint-disable-line no-unused-vars

const MovieTitle = ({ n }) => {
  return <div>MovieTitle {n}</div>
}

MovieTitle.propTypes = {}
MovieTitle.defaultProps = {}

export default MovieTitle
